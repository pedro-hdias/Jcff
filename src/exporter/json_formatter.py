def format_json(estrutura):
    """
    Neste estágio, a estrutura já está formatada corretamente pelo scanner.
    Essa função existe para deixar o caminho aberto a transformações futuras,
    como ordenações personalizadas, metadados ou contadores globais.
    """
    return estrutura
